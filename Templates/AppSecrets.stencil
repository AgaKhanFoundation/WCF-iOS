struct AppSecrets {
    static let devPassword = "{{ argument.devPassword }}"
    static let stagingPassword = "{{ argument.stagingPassword }}"
    static let prodPassword = "{{ argument.prodPassword }}"
}
