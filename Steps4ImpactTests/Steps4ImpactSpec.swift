import Quick
import Nimble
@testable import Steps4Impact

class Steps4ImpactSpec: QuickSpec {
  override func spec() {
    describe("Steps4Impact") {
      it("should run tests") {
        expect(true).to(beTrue())
      }
    }
  }
}
